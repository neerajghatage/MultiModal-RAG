apiVersion: v1
kind: Secret
metadata:
  name: rag-system-secrets
  namespace: rag-system
  labels:
    app.kubernetes.io/name: rag-system
type: Opaque
stringData:
  # ─────────────────────────────────────────────────────────────────────
  # DEPRECATED: All secrets have been migrated to Azure Key Vault
  # (kv-rag-system). This file is kept only for reference.
  # Secrets are now injected via Secrets Store CSI Driver.
  # See: secret-provider-class.yaml
  # ─────────────────────────────────────────────────────────────────────

  # ── Qdrant (no auth for in-cluster Qdrant) ──
  QDRANT_API_KEY: ""
